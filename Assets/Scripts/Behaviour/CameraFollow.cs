using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class CameraFollow : MonoBehaviour
{
    /*
    Vector3 offset;
    CharacterBehaviour target;

    private void Start()
    {
        offset = transform.position - CharacterBehaviour.instance.transform.position;
        target = CharacterBehaviour.instance.transform.GetComponent<CharacterBehaviour>();
    }

    private void Update()
    {
        Vector3 pos = target.cameraTarget.transform.position + offset;
        Vector3 localPos = new Vector3(pos.x , transform.position.y , pos.z);
        transform.position = localPos;
    }
    */
}
